---
import { buildAppIndexPaths } from '@/domain/app/paths';
import PrivacyPage from './[lang].astro';

export function getStaticPaths() {
  return buildAppIndexPaths();
}

const { app } = Astro.params;
---

<PrivacyPage app={app} lang='ja' />
