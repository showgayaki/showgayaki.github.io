---
import BaseLayout from '@/layouts/BaseLayout.astro';
import ja from '@/i18n/privacy/ja';
import en from '@/i18n/privacy/en';

type Props = { lang: string };

export function getStaticPaths() {
  return [
    { params: { lang: 'ja' } },
    { params: { lang: 'en' } }
  ];
}

const lang = Astro.props.lang ?? Astro.params.lang ?? 'ja';
const dict = lang === 'ja' ? ja : en;
---

<BaseLayout page="/privacy" lang={lang} mainClass="legal">
    <h2 class="legal__title">{dict.title}</h2>

    {
        dict.sections.map((section) => (
            <section class="legal__section">
                <h3 class="legal__section-title">{section.title}</h3>

                {section.paragraphs?.map((p) => (
                    <p class="legal__explanation">{p}</p>
                ))}

                {section.list && (
                    <ul class="legal__list legal__list--style-disc">
                        {section.list.map((item) => (
                            <li>{item}</li>
                        ))}
                    </ul>
                )}

                {section.subsections?.map((sub) => (
                    <section class="legal__subsection">
                        <h4 class="legal__subsection-title">{sub.title}</h4>

                        {sub.paragraphs?.map((p) => (
                            <p class="legal__explanation">{p}</p>
                        ))}

                        {sub.list && (
                            <ul class="legal__list legal__list--style-disc">
                                {sub.list.map((item) => (
                                    <li>{item}</li>
                                ))}
                            </ul>
                        )}

                        {sub.links && (
                            <ul class="legal__list legal__list--style-none">
                                {sub.links.map((link) => (
                                    <li>
                                        <a href={link.href} target="_blank">
                                            {link.label}
                                            <i class="fas fa-external-link-alt"></i>
                                        </a>
                                    </li>
                                ))}
                            </ul>
                        )}
                    </section>
                ))}

                {section.contact && (
                    <p class="legal__explanation">
                        E-mail:{' '}
                        <a href={`mailto:${section.contact}`}>
                            {section.contact}
                        </a>
                    </p>
                )}
            </section>
        ))
    }
</BaseLayout>
