---
import { buildAppIndexPaths } from '@/domain/app/paths';
import TermPage from './[lang].astro';

export function getStaticPaths() {
  return buildAppIndexPaths('terms');
}

const { app } = Astro.params;
---

<TermPage app={app} lang='ja' />
