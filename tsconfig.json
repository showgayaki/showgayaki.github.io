{
    "extends": "astro/tsconfigs/strict",
    "compilerOptions": {
        // ベースとなるディレクトリ（通常はルート）
        "baseUrl": "./",
        "paths": {
            "@/*": [
                "src/*"
            ]
        }
    }
}